apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-google-workspace

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml

commonLabels:
  app.kubernetes.io/part-of: mcp-google-workspace
  app.kubernetes.io/managed-by: kustomize
