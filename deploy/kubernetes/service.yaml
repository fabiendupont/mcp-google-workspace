apiVersion: v1
kind: Service
metadata:
  name: mcp-google-workspace
  namespace: mcp-google-workspace
  labels:
    app.kubernetes.io/name: mcp-google-workspace
    app.kubernetes.io/component: server
spec:
  selector:
    app.kubernetes.io/name: mcp-google-workspace
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
